@model IEnumerable<SIGEC.Models.Medicine>

@{
    ViewBag.Title = _("Medicaments_Title");
}

<div class="row">
    <div class="col-lg-12">
        <hgroup class="title">
            <h1>
                <img alt="medicaments_picture" src="~/Images/medicaments.png" />
                @ViewBag.Title.</h1>
            <h2></h2>
        </hgroup>
        <div class="well">
            <fieldset>
                <legend>@_("Medicaments_IndexLegend")</legend>
                <div class="form-group col-lg-12">
                    <p>
                        @Html.ActionLink(_("lblNew"), "Create", null, new { @class = "btn btn-primary fr mb10" })
                    </p>
                </div>
                <div class="form-group col-lg-12 scroll">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.name)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.type)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.usage)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.dosage)
                                </th>
                                <th>@_("lblOptions")</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.name)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.type)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.usage)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.dosage)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Edit", "Edit", new { id = item.ID }, new { @class = "button_link edit_link" })
                                        @Html.ActionLink("Details", "Details", new { id = item.ID }, new { @class = "button_link view_link" })
                                        @Html.ActionLink("Delete", "Delete", new { id = item.ID }, new { @class = "button_link delete_link" })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </fieldset>
        </div>
    </div>
</div>
